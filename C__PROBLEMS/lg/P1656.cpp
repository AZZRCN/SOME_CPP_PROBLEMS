#include <bits/stdc++.h>
using namespace std;
vector<pair<int,int>> us;
unordered_set<int> vs;
int n,m,a,b;
int main(){
    cin >> n >> m;
    for(int i = 1; i <= m; i++){
        cin >> a >> b;
        us.push_back(make_pair(a,b));
    }
    sort(us.begin(),us.end());
    for(int i = 0; i < m; i++){
        
    }
}